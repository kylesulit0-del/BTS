---
phase: 04-config-driven-ui
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - src/components/FeedFilter.tsx
  - src/components/BiasFilter.tsx
  - src/components/NewsCard.tsx
  - src/pages/Home.tsx
  - src/pages/Tours.tsx
  - src/pages/News.tsx
  - vite.config.ts
  - index.html
  - public/manifest.json
  - src/config/groups/example/index.ts
  - src/data/events.ts
  - src/data/news.ts
  - src/data/members.ts
autonomous: true
requirements: [CONFIG-03, CONFIG-04]

must_haves:
  truths:
    - "FeedFilter tabs are generated dynamically from config.sources unique types -- no hardcoded filter array"
    - "BiasFilter label reads from config.labels.memberFilterLabel"
    - "Home page quote reads from config.labels.homeQuote"
    - "Tours page title and subtitle read from config.labels"
    - "News page fallback reads from config.news"
    - "PWA manifest is generated from config at build time -- no static manifest.json"
    - "index.html title and apple-mobile-web-app-title come from config at build time"
    - "An example group config exists as a permanent template"
    - "grep for hardcoded BTS references in src/ (excluding config/groups/) returns zero matches"
  artifacts:
    - path: "src/components/FeedFilter.tsx"
      provides: "Config-derived filter tabs"
      contains: "getConfig"
    - path: "vite.config.ts"
      provides: "PWA manifest generation and index.html title injection from config"
      contains: "manifest:"
    - path: "src/config/groups/example/index.ts"
      provides: "Template group config for clone-and-swap"
      contains: "satisfies GroupConfig"
  key_links:
    - from: "src/components/FeedFilter.tsx"
      to: "src/config/index.ts"
      via: "getConfig().sources and getConfig().labels.sourceLabels"
      pattern: "getConfig.*sources"
    - from: "vite.config.ts"
      to: "src/config/groups/bts/index.ts"
      via: "import config for manifest and HTML injection"
      pattern: "import.*config"
    - from: "src/pages/Tours.tsx"
      to: "src/config/index.ts"
      via: "getConfig().labels.tourTitle and config.events"
      pattern: "config\\.labels\\.tourTitle"
---

<objective>
Wire all UI components and build artifacts to read from config, create example group config, delete legacy data files, and audit for remaining hardcoded BTS references.

Purpose: Complete the clone-and-swap architecture so changing the config import swaps the entire app.
Output: Fully config-driven UI, config-generated PWA manifest, example group template, zero hardcoded BTS references outside config.
</objective>

<execution_context>
@/home/kylesulit/.claude/get-shit-done/workflows/execute-plan.md
@/home/kylesulit/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-config-driven-ui/04-RESEARCH.md
@.planning/phases/04-config-driven-ui/04-01-SUMMARY.md
@src/components/FeedFilter.tsx
@src/components/BiasFilter.tsx
@src/pages/Home.tsx
@src/pages/Tours.tsx
@src/pages/News.tsx
@src/components/NewsCard.tsx
@vite.config.ts
@index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire UI components and pages to read from config</name>
  <files>
    src/components/FeedFilter.tsx
    src/components/BiasFilter.tsx
    src/pages/Home.tsx
    src/pages/Tours.tsx
    src/pages/News.tsx
    src/components/NewsCard.tsx
    src/data/events.ts
    src/data/news.ts
    src/data/members.ts
  </files>
  <action>
1. **FeedFilter.tsx** -- Replace hardcoded `filters` array with config-derived tabs (CONFIG-03):
   - Import `getConfig` from `../config`
   - Remove the static `filters` array (lines 8-15)
   - Inside the component, derive unique source types from `config.sources` in config-defined order:
     ```
     const config = getConfig();
     const sourceTypes: { type: string; label: string }[] = [];
     const seen = new Set<string>();
     for (const source of config.sources) {
       if (source.enabled !== false && !seen.has(source.type)) {
         seen.add(source.type);
         sourceTypes.push({ type: source.type, label: config.labels.sourceLabels[source.type] });
       }
     }
     ```
   - Render "All" tab first (hardcoded behavior per locked decision), then map `sourceTypes` for remaining tabs
   - Update `FeedFilterProps` to use `string | "all"` instead of `FeedSource | "all"` (since FeedSource is now string, this is technically the same, but be explicit)

2. **BiasFilter.tsx** -- Replace hardcoded "Select Your Bias" label:
   - Already imports `getConfig` -- add: read `config.labels.memberFilterLabel`
   - Replace the hardcoded string `"Select Your Bias"` on line 21 with `{config.labels.memberFilterLabel}`
   - Remove the `as BiasId` cast on `m.id` (BiasId is now string, `m.id` is already string)

3. **Home.tsx** -- Replace hardcoded quote:
   - Already imports `getConfig` and reads config
   - Replace the hardcoded `"Love yourself, speak yourself"` on line 61 with `{config.labels.homeQuote}`

4. **Tours.tsx** -- Replace hardcoded title/subtitle and data source:
   - Import `getConfig` from `../config`
   - Remove `import { events } from "../data/events"` -- use `config.events` instead
   - Replace `"BTS WORLD TOUR 'ARIRANG'"` (line 25) with `{config.labels.tourTitle}`
   - Replace `"82+ shows across 34 cities in 23 countries"` (line 26) with `{config.labels.tourSubtitle}`
   - Use `config.events` instead of `events` in the sort/group logic

5. **News.tsx** -- Replace data import with config:
   - Remove `import { news } from "../data/news"` -- use `config.news` instead
   - Already has `const config = getConfig()` -- use `config.news` in the fallback rendering section

6. **NewsCard.tsx** -- Update type import:
   - Change `import type { NewsItem } from "../data/news"` to `import type { NewsItem } from "../config/types"`
   - (The NewsItem type is now defined in config/types.ts from Plan 01)

7. **Delete legacy data files** that are now unused:
   - Delete `src/data/members.ts` (already confirmed no imports -- legacy file)
   - Delete `src/data/events.ts` (imports removed above)
   - Delete `src/data/news.ts` (imports removed above)
   - If `src/data/` directory is now empty, leave it (git tracks files not dirs)
  </action>
  <verify>Run `npx tsc --noEmit` -- zero type errors. Run `npm run build` -- successful build. Grep for `from "../data/` in src/ to confirm no remaining imports of deleted files.</verify>
  <done>FeedFilter tabs derived from config.sources. BiasFilter label from config. Home quote from config. Tours title/subtitle and events from config. News fallback from config. All legacy data files deleted.</done>
</task>

<task type="auto">
  <name>Task 2: Config-driven build artifacts and example group config</name>
  <files>
    vite.config.ts
    index.html
    public/manifest.json
    src/config/groups/example/index.ts
  </files>
  <action>
1. **vite.config.ts** -- Generate PWA manifest from config and inject HTML title:
   - Import the BTS config at the top of the file. Since vite.config.ts runs in Node.js at build time, import directly: `import { btsConfig } from "./src/config/groups/bts/index.ts"` (the config files are pure data, no browser APIs -- confirmed in research)
   - Replace `manifest: false` with a manifest object using config values:
     ```
     manifest: {
       name: btsConfig.labels.appName,
       short_name: btsConfig.theme.groupName,
       description: btsConfig.labels.appDescription,
       theme_color: "#0d0d0d",
       background_color: "#0d0d0d",
       display: "standalone",
       scope: "/",
       start_url: "/",
       icons: [
         { src: "/favicon.svg", sizes: "any", type: "image/svg+xml", purpose: "any" },
       ],
     }
     ```
   - Add a custom Vite plugin in the `plugins` array to inject config into index.html:
     ```
     {
       name: "inject-config-html",
       transformIndexHtml(html) {
         return html
           .replace(/<title>.*<\/title>/, `<title>${btsConfig.labels.appTitle}</title>`)
           .replace(/content="BTS"/, `content="${btsConfig.theme.groupName}"`);
       },
     }
     ```
   - NOTE: We import `btsConfig` directly (not via `config/index.ts`) in vite.config.ts because the config swap point is `config/index.ts` -- when you swap groups, you change the import there, and vite.config.ts should also be updated to import from the new group. Alternatively, import from `config/index.ts` -- either works, but direct import makes the swap point explicit.

   Actually, import from `./src/config/index.ts` so there is ONE swap point. Change `config/index.ts` and both runtime and build-time pick up the new config:
   ```
   import { config } from "./src/config/index.ts";
   ```
   Then use `config.labels.appName`, `config.theme.groupName`, etc.

2. **index.html** -- Remove hardcoded values that will be injected:
   - Replace `<title>BTS - ARMY App</title>` with `<title>%APP_TITLE%</title>` -- actually NO, the transformIndexHtml plugin handles this at build time. Leave the hardcoded values as development defaults; the plugin overwrites them during build. This way `vite dev` still shows reasonable titles.
   - Actually, for consistency, leave the HTML as-is. The `transformIndexHtml` hook replaces the values at build time and during dev server. So the static HTML values are just fallbacks visible only when viewing the raw file.
   - Remove `<link rel="manifest" href="/manifest.json" />` -- vite-plugin-pwa injects the manifest link automatically when `manifest` is an object (not `false`).

3. **Delete `public/manifest.json`** -- It would override the vite-plugin-pwa generated manifest since Vite copies public/ files as-is. The manifest is now generated from config.

4. **Create `src/config/groups/example/index.ts`** -- Permanent example/template config:
   - Import all types from `../../types.ts` (GroupConfig, GroupLabels, Event, NewsItem, MemberConfig, SourceEntry, ThemeConfig)
   - Create a complete, minimal example config with:
     - 1 placeholder member with all required MemberConfig fields
     - 1 placeholder source (reddit type) with all required SourceEntry fields
     - Complete theme with placeholder values
     - Keywords regex with placeholder group term
     - Full labels object with placeholder values for all GroupLabels fields
     - 1 placeholder event and 1 placeholder news item
   - Use `satisfies GroupConfig` for compile-time validation
   - Add a doc comment explaining: "Copy this directory to create a new group. All fields are required -- TypeScript will error if any are missing. Then change the import in src/config/index.ts to point to your new group."

5. **Final audit** -- Run `grep -ri "bts\|bangtan\|army\|방탄" src/ --include="*.ts" --include="*.tsx" --exclude-dir="config/groups"` to find any remaining hardcoded BTS references. Expected results:
   - `src/config/index.ts` -- imports from `./groups/bts/` (expected, this is the swap point)
   - Any other matches must be extracted to config
   - If unexpected matches found, fix them in this task
  </action>
  <verify>
1. `npm run build` -- successful production build
2. Check build output for generated manifest: `cat dist/manifest.webmanifest` (or similar) should show config-derived values
3. `ls public/manifest.json` should fail (deleted)
4. `npx tsc --noEmit` -- zero type errors including example config
5. `grep -ri "bts\|bangtan\|army\|방탄" src/ --include="*.ts" --include="*.tsx" --exclude-dir="config/groups"` -- only match should be the import in config/index.ts
  </verify>
  <done>PWA manifest generated from config. index.html title/meta injected from config at build time. Static manifest.json deleted. Example group config exists as permanent template. Audit confirms zero hardcoded BTS references outside config/groups/ (except the swap-point import).</done>
</task>

</tasks>

<verification>
1. `npm run build` passes -- production build succeeds
2. FeedFilter tabs match source types defined in BTS config (reddit, youtube, rss, twitter, tumblr) plus "All"
3. No file in `src/data/` is imported by any code (all deleted or unused)
4. `public/manifest.json` does not exist
5. Build output manifest contains config-derived name/description
6. `npx tsc --noEmit` passes including the example config
7. Hardcoded BTS audit: `grep -ri "bts\|bangtan\|army" src/ --include="*.ts" --include="*.tsx" --exclude-dir="config/groups"` returns only `src/config/index.ts`
</verification>

<success_criteria>
- FeedFilter tabs are fully config-driven -- adding a new source type to config automatically creates a filter tab
- All user-facing text that references the group comes from config.labels
- PWA manifest and HTML meta are generated from config at build time
- Example group config compiles as valid GroupConfig, serving as documentation and template
- Changing the import in `src/config/index.ts` (and the corresponding vite.config.ts import) to a different group swaps the entire app with zero code changes
- Zero hardcoded BTS references exist outside `src/config/groups/bts/`
</success_criteria>

<output>
After completion, create `.planning/phases/04-config-driven-ui/04-02-SUMMARY.md`
</output>
