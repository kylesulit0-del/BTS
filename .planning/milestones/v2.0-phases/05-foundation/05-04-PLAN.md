---
phase: 05-foundation
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/server/src/scheduler.ts
  - packages/server/src/routes/feed.ts
autonomous: true
gap_closure: true
requirements:
  - INFRA-04
  - API-01

must_haves:
  truths:
    - "Scrape schedule defaults to every 20 minutes and is overridable via CRON_SCHEDULE env var"
    - "GET /api/feed/:id returns a single content item by ID"
    - "GET /api/feed/:id returns 404 for nonexistent items"
  artifacts:
    - path: "packages/server/src/scheduler.ts"
      provides: "Configurable cron schedule via CRON_SCHEDULE env var"
      contains: "process.env.CRON_SCHEDULE"
    - path: "packages/server/src/routes/feed.ts"
      provides: "Single-item endpoint GET /feed/:id"
      contains: "/:id"
  key_links:
    - from: "packages/server/src/scheduler.ts"
      to: "node-cron"
      via: "cron.schedule with env-driven expression"
      pattern: "process\\.env\\.CRON_SCHEDULE"
    - from: "packages/server/src/routes/feed.ts"
      to: "packages/server/src/db/schema.ts"
      via: "Drizzle query by ID"
      pattern: "eq\\(contentItems\\.id"
---

<objective>
Close 2 verification gaps from Phase 5: make the cron schedule configurable with a 20-minute default (INFRA-04 compliance), and add the missing single-item feed endpoint (API-01 completeness).

Purpose: VERIFICATION.md found 2 gaps -- hardcoded hourly cron violates INFRA-04's "configurable 15-30 minute intervals," and GET /api/feed/:id is absent despite API-01 requiring it.
Output: Updated scheduler.ts and feed.ts that fully satisfy INFRA-04 and API-01.
</objective>

<execution_context>
@/home/kylesulit/.claude/get-shit-done/workflows/execute-plan.md
@/home/kylesulit/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-foundation/05-VERIFICATION.md
@.planning/phases/05-foundation/05-03-SUMMARY.md
@packages/server/src/scheduler.ts
@packages/server/src/routes/feed.ts
@packages/server/src/db/schema.ts
@packages/server/src/db/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make cron schedule configurable with 20-minute default</name>
  <files>packages/server/src/scheduler.ts</files>
  <action>
Update scheduler.ts to read a CRON_SCHEDULE environment variable instead of using the hardcoded '0 * * * *' expression:

1. At the top of startScheduler(), read `process.env.CRON_SCHEDULE` with fallback `'*/20 * * * *'` (every 20 minutes, within INFRA-04's 15-30 min range).
2. Validate the cron expression using `cron.validate(schedule)` before passing to `cron.schedule()`. If invalid, log a warning and fall back to the default.
3. Update the console.log messages to print the actual schedule being used (not hardcoded text).
4. Update the JSDoc comment to reflect configurability.

The change is isolated to scheduler.ts -- no other files need updating.

Gap reason: INFRA-04 requires "configurable 15-30 minute intervals." The current implementation is hardcoded to hourly.
  </action>
  <verify>
Read the updated scheduler.ts and confirm:
- `process.env.CRON_SCHEDULE` is read with `'*/20 * * * *'` as the default
- `cron.validate()` is called on the schedule string
- The log message includes the actual schedule expression
  </verify>
  <done>Scheduler defaults to every 20 minutes and respects CRON_SCHEDULE env var override. Invalid cron expressions fall back to the default with a warning.</done>
</task>

<task type="auto">
  <name>Task 2: Add single-item feed endpoint GET /api/feed/:id</name>
  <files>packages/server/src/routes/feed.ts</files>
  <action>
Add a GET /feed/:id route inside the existing registerFeedRoutes function in feed.ts:

1. Add a new route `server.get<{ Params: { id: string } }>('/feed/:id', ...)` after the existing GET /feed route.
2. Parse the id param as an integer. If NaN, return 400 with `{ error: 'Invalid item ID' }`.
3. Query `db.select().from(contentItems).where(eq(contentItems.id, id)).get()`.
4. If no row found, return 404 with `{ error: 'Item not found' }`.
5. If found, map the row to FeedItem shape (reuse the same mapping logic as the feed list endpoint) and return it directly as the response body (not wrapped in items array).

This completes the API-01 requirement: "paginated feed endpoint, single-item endpoint, and source health endpoint."

Gap reason: API-01 requires a single-item endpoint. The paginated feed and health endpoints exist but GET /api/feed/:id was never implemented.
  </action>
  <verify>
Read the updated feed.ts and confirm:
- A GET /feed/:id route exists inside registerFeedRoutes
- It parses the :id param, queries contentItems by id, and returns the item or 404
- The FeedItem mapping is consistent with the list endpoint
- Build passes: cd /home/kylesulit/bts && npx tsc --noEmit --project packages/server/tsconfig.json
  </verify>
  <done>GET /api/feed/:id returns a single FeedItem by ID (200), returns 404 for missing items, and returns 400 for invalid IDs.</done>
</task>

</tasks>

<verification>
1. Read updated scheduler.ts -- confirm CRON_SCHEDULE env var with */20 default and validation
2. Read updated feed.ts -- confirm GET /feed/:id route with proper error handling
3. TypeScript compiles without errors: `npx tsc --noEmit --project packages/server/tsconfig.json`
</verification>

<success_criteria>
- scheduler.ts reads CRON_SCHEDULE env var, defaults to '*/20 * * * *', validates with cron.validate()
- GET /api/feed/:id returns single FeedItem (200), 404 for missing, 400 for invalid ID
- TypeScript compilation passes
- Both INFRA-04 and API-01 verification gaps are closed
</success_criteria>

<output>
After completion, create `.planning/phases/05-foundation/05-04-SUMMARY.md`
</output>
