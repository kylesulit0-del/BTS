---
phase: 10-snap-feed-core
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [SNAP-01, SNAP-02, SNAP-03, SNAP-04, SNAP-05, SNAP-06, SNAP-07, PERF-01]
gap_closure: true

must_haves:
  truths:
    - "SNAP-04 requirement text matches the implemented DOM_WINDOW_SIZE=5 design decision"
    - "REQUIREMENTS.md SNAP-04 reads 'configurable number of items (default 5)' not '3 items'"
    - "ROADMAP.md Phase 10 success criteria and goal text reflect configurable window size, not hardcoded 3"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated SNAP-04 requirement text"
      contains: "configurable number"
    - path: ".planning/ROADMAP.md"
      provides: "Updated Phase 10 goal and success criteria"
      contains: "configurable number"
  key_links: []
---

<objective>
Reconcile SNAP-04 requirement text with the implemented design decision to use DOM_WINDOW_SIZE=5.

Purpose: The verification report found that SNAP-04 in REQUIREMENTS.md says "3 items rendered in DOM" but the implementation uses a configurable window size defaulting to 5. This was a conscious design decision made during research (5 provides smoother scrolling without significant DOM overhead). The requirement text was never updated to reflect this. This plan updates the stale documentation.

Output: Updated REQUIREMENTS.md and ROADMAP.md with correct SNAP-04 language.
</objective>

<execution_context>
@/home/kylesulit/.claude/get-shit-done/workflows/execute-plan.md
@/home/kylesulit/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/10-snap-feed-core/10-VERIFICATION.md
@packages/frontend/src/config/snap.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update SNAP-04 requirement text and Phase 10 goal in REQUIREMENTS.md and ROADMAP.md</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <action>
Update SNAP-04 in REQUIREMENTS.md from:
  "User sees only 3 items rendered in DOM at any time (current/prev/next) for performance"
to:
  "User sees only a configurable number of items rendered in DOM at any time (default 5 — current + 2 prev + 2 next) for performance"

Update ROADMAP.md Phase 10 in two places:
1. Phase goal text — change "only 3 items exist in the DOM at any time" to "only a configurable number of items exist in the DOM at any time (default 5)"
2. Success criteria item about DOM virtualization — update from "3" to "configurable (default 5)"

Do NOT change any other text. These are targeted edits to align documentation with the implemented design decision (DOM_WINDOW_SIZE=5 in packages/frontend/src/config/snap.ts).
  </action>
  <verify>
Grep REQUIREMENTS.md and ROADMAP.md for "3 items" — should return zero matches related to SNAP-04 or DOM virtualization.
Grep both files for "configurable" — should find the updated SNAP-04 text.
  </verify>
  <done>SNAP-04 requirement text says "configurable number of items (default 5)" matching the implementation. ROADMAP Phase 10 goal and success criteria reflect the same. No references to "3 items" remain in SNAP-04 context.</done>
</task>

</tasks>

<verification>
- REQUIREMENTS.md SNAP-04 matches implementation (DOM_WINDOW_SIZE=5)
- ROADMAP.md Phase 10 goal references configurable window size
- No other requirements or roadmap entries were accidentally modified
</verification>

<success_criteria>
SNAP-04 verification status changes from PARTIAL to SATISFIED — requirement text matches implementation.
</success_criteria>

<output>
After completion, create `.planning/phases/10-snap-feed-core/10-04-SUMMARY.md`
</output>
