---
phase: 06-scraper-expansion
plan: 04
type: execute
wave: 3
depends_on:
  - 06-02
  - 06-03
files_modified:
  - packages/server/src/routes/health.ts
  - packages/server/src/scrapers/base.ts
  - packages/frontend/src/pages/Status.tsx
  - packages/frontend/src/components/Navbar.tsx
  - packages/frontend/src/components/FeedCard.tsx
  - packages/frontend/src/App.tsx
autonomous: false
requirements:
  - SCRAPE-07

must_haves:
  truths:
    - "Frontend status page shows traffic light indicators (green/yellow/red) per source based on recent scrape health"
    - "Feed cards display thumbnail images from all sources and gracefully hide broken images"
    - "Health API returns recent run history per source with success/empty/error counts"
    - "scrape_runs older than 7 days are cleaned up after each scrape cycle"
  artifacts:
    - path: "packages/server/src/routes/health.ts"
      provides: "Enhanced health endpoint with per-source run history and traffic light data"
      contains: "recentRuns"
    - path: "packages/frontend/src/pages/Status.tsx"
      provides: "Status page with traffic light indicators per source"
      contains: "traffic"
    - path: "packages/frontend/src/components/FeedCard.tsx"
      provides: "Feed card with thumbnail display and broken image handling"
      contains: "onError"
  key_links:
    - from: "packages/frontend/src/pages/Status.tsx"
      to: "packages/server/src/routes/health.ts"
      via: "Fetches /api/health/sources for status data"
      pattern: "fetch.*health"
    - from: "packages/frontend/src/components/FeedCard.tsx"
      to: "packages/shared/src/types/feed.ts"
      via: "Uses FeedItem.thumbnailUrl for image display"
      pattern: "thumbnailUrl"
---

<objective>
Add scrape health status page with traffic light indicators, enhance feed cards with thumbnail display and broken image handling, and add scrape_runs cleanup.

Purpose: Completes the observability story for Phase 6 — users can see at a glance which sources are healthy. Feed cards display rich media from all sources. Broken images degrade gracefully.

Output: Frontend status page, enhanced feed cards with thumbnails, enhanced health API, scrape_runs history cleanup.
</objective>

<execution_context>
@/home/kylesulit/.claude/get-shit-done/workflows/execute-plan.md
@/home/kylesulit/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-scraper-expansion/06-RESEARCH.md
@.planning/phases/06-scraper-expansion/06-01-SUMMARY.md
@.planning/phases/06-scraper-expansion/06-02-SUMMARY.md
@.planning/phases/06-scraper-expansion/06-03-SUMMARY.md

@packages/server/src/routes/health.ts
@packages/server/src/scrapers/base.ts
@packages/frontend/src/components/FeedCard.tsx
@packages/frontend/src/App.tsx
@packages/frontend/src/components/Navbar.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance health API and add scrape_runs cleanup</name>
  <files>
    packages/server/src/routes/health.ts
    packages/server/src/scrapers/base.ts
  </files>
  <action>
**Enhance health route in `packages/server/src/routes/health.ts`:**

Update the existing `/health` endpoint to return richer data for the status page:

1. Add a new endpoint `GET /health/sources` that returns per-source scrape history:
   - For each distinct source, query the last 10 runs (ordered by startedAt DESC)
   - Calculate traffic light status:
     - Count runs in last 24 hours
     - If no runs in 24 hours: `'red'`
     - If any error in last 3 runs: count errors in last 10 runs. If >50% errors: `'red'`. If any errors but <50%: `'yellow'`. All success/empty: `'green'`.
     - `'empty'` status counts as `'green'` (fetch worked, just no new items)
   - Return per source: `{ source, status: 'green'|'yellow'|'red', lastRun: ISO string, lastStatus: 'success'|'empty'|'error', itemsFound: number, recentRuns: Array<{status, startedAt, itemsFound, duration}> }`
   - Return overall: `{ sources: [...], uptime: process.uptime(), lastScrapeAt: ISO string }`

2. Keep existing `/health` endpoint unchanged for backward compatibility.

3. Import `and, gte, isNull, desc, eq, sql, lt` as needed from drizzle-orm.

**Add scrape_runs cleanup in `packages/server/src/scrapers/base.ts`:**

At the end of `runAllScrapers()`, after the content cleanup (soft delete), add scrape_runs cleanup:

```typescript
// Clean up scrape_runs older than 7 days
const sevenDaysAgo = new Date(Date.now() - 7 * 24 * 60 * 60 * 1000);
const deletedRuns = db.delete(scrapeRuns)
  .where(lt(scrapeRuns.startedAt, sevenDaysAgo))
  .run();
if (deletedRuns.changes > 0) {
  console.log(`[scraper] Cleaned up ${deletedRuns.changes} scrape_runs older than 7 days`);
}
```

This is a hard delete (not soft delete) since scrape_runs is operational metadata, not user content.
  </action>
  <verify>
1. `npx tsc --noEmit` passes in packages/server
2. `curl http://localhost:3001/api/health/sources` returns per-source data with traffic light status
3. Existing `curl http://localhost:3001/api/health` still works
  </verify>
  <done>
Health API returns per-source traffic light status with recent run history. scrape_runs older than 7 days are cleaned up after each scrape cycle.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add frontend status page and feed card thumbnails with broken image handling</name>
  <files>
    packages/frontend/src/pages/Status.tsx
    packages/frontend/src/components/FeedCard.tsx
    packages/frontend/src/components/Navbar.tsx
    packages/frontend/src/App.tsx
  </files>
  <action>
**Create status page at `packages/frontend/src/pages/Status.tsx`:**

Build a simple, phone-friendly status page with traffic light indicators:

1. Fetch from `${import.meta.env.VITE_API_URL}/api/health/sources` on mount. If `VITE_API_URL` is not set, show a message: "Status page requires API connection (set VITE_API_URL)".
2. Display each source as a card with:
   - Traffic light circle (CSS — colored circle 16px diameter: green=#22c55e, yellow=#eab308, red=#ef4444)
   - Source name (bold)
   - Last run time (relative — "2 min ago", "1 hour ago")
   - Items found in last run
   - Last status text
3. Layout: single column, stacked cards, max-width 600px, centered. Phone-first design per user decision ("quick-glance friendly from a phone — traffic lights, not data tables").
4. Loading state: skeleton cards (3 placeholder cards with pulsing animation, similar to existing SkeletonCard pattern).
5. Auto-refresh every 60 seconds.
6. Style with inline styles or CSS module (follow existing component patterns in the codebase).

**Add route in `packages/frontend/src/App.tsx`:**

Add a route for `/status` that renders the Status page. Import the Status component. Follow existing routing pattern (check how other pages like Members, Tours are routed).

**Add nav link in `packages/frontend/src/components/Navbar.tsx`:**

Add a "Status" link in the navigation that points to `/status`. Place it at the end of the nav items.

**Update feed cards in `packages/frontend/src/components/FeedCard.tsx`:**

1. The FeedCard component receives a FeedItem (or equivalent prop). Add thumbnail display:
   - If `item.thumbnailUrl` exists, render an `<img>` tag at the top of the card
   - Image styling: `width: 100%`, `aspect-ratio: 16/9`, `object-fit: cover`, `border-radius` matching card style
   - Add `loading="lazy"` for performance
2. **Broken image handling per user decision:** Add an `onError` handler on the `<img>` tag:
   - On error, set a local state flag `imageError` to true
   - When `imageError` is true, hide the entire image area (don't render the `<img>` tag at all — card becomes text-only with text preview filling the space naturally)
   - Use `useState(false)` for the error flag
3. If `item.thumbnailUrl` is null/undefined or `imageError` is true, do NOT render any image area. The card should be text-only with the text preview filling naturally.
4. Display engagement stats if `item.engagementStats` exists:
   - For Reddit: show upvotes + comments icons (may already exist from v1.0)
   - For Bluesky: show heart icon + likes count
   - For sources with null engagementStats: show nothing (per user decision: "feed cards simply don't show engagement for those sources")

**Important:** Check existing FeedCard.tsx for current engagement display implementation. It may already show score/commentCount. Keep the existing display working for Reddit and add new display for the engagementStats JSON field. The existing `score`/`commentCount` fields on FeedItem are still populated for backward compatibility.
  </action>
  <verify>
1. `npx tsc --noEmit` passes in packages/frontend
2. Build frontend: `npm run build` in packages/frontend — no errors
3. Navigate to `/status` in browser — shows traffic light indicators for each source
4. Feed cards display thumbnails for YouTube/RSS/Tumblr items
5. Deliberately set a bad thumbnail URL — verify image area collapses on load failure
  </verify>
  <done>
Status page shows per-source traffic light health indicators, accessible from Navbar. Feed cards display thumbnails from all sources and gracefully hide broken images. Engagement stats display for Bluesky (likes) and continue working for Reddit.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify scraper expansion end-to-end</name>
  <files>packages/server/src/index.ts</files>
  <action>
This is a verification checkpoint. No code changes needed. The user verifies:
- Complete Phase 6 scraper expansion: YouTube, RSS/news, Tumblr, and Bluesky scrapers producing content in the database with thumbnails and engagement stats
- Status page with traffic light health indicators
- Feed cards with thumbnail display and broken image handling
  </action>
  <verify>
1. Start the server: `cd packages/server && npm run dev`
2. Trigger a scrape: `curl -X POST http://localhost:3001/api/scrape`
3. Check feed items: `curl http://localhost:3001/api/feed | jq '.items[:3]'` — verify items from multiple sources (youtube, rss, tumblr) with thumbnailUrl and engagementStats fields
4. Check health: `curl http://localhost:3001/api/health/sources` — verify per-source traffic light data
5. Start the frontend: `cd packages/frontend && npm run dev`
6. Open the feed in browser — verify feed cards show thumbnails for YouTube/RSS items
7. Navigate to `/status` — verify traffic light indicators per source
8. If Bluesky credentials are set: verify bluesky items appear in feed with like counts
  </verify>
  <done>User confirms all sources produce content, status page works, feed cards display thumbnails correctly. Type "approved" or describe issues to fix.</done>
</task>

</tasks>

<verification>
1. Health API returns traffic light status per source
2. Status page renders on `/status` with source health indicators
3. Feed cards display thumbnails and handle broken images gracefully
4. scrape_runs cleanup removes entries older than 7 days
5. Engagement stats display correctly for Reddit and Bluesky sources
6. Sources with no engagement show no engagement UI (YouTube RSS, news, Tumblr)
</verification>

<success_criteria>
- Status page provides at-a-glance health of all scrape sources
- Feed cards display thumbnails from all source types
- Broken thumbnails collapse gracefully without blank image areas
- Health API serves data for the status page
- All Phase 6 requirements satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/06-scraper-expansion/06-04-SUMMARY.md`
</output>
