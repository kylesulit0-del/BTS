---
phase: 13-fixed-header-sort-bottom-sheet
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/frontend/src/components/snap/FixedHeader.tsx
  - packages/frontend/src/components/snap/SortSheet.tsx
  - packages/frontend/src/App.css
autonomous: true
requirements:
  - NAV-01
  - NAV-02
  - NAV-03
  - NAV-04

must_haves:
  truths:
    - "FixedHeader component renders 'Army Feed' branding text and two icon buttons (sort, filter)"
    - "SortSheet component renders all 5 sort options with checkmark on active option"
    - "SortSheet uses identical portal/backdrop/animation pattern as FilterSheet"
    - "CSS styles for header and sort sheet match the dark theme and follow existing conventions"
  artifacts:
    - path: "packages/frontend/src/components/snap/FixedHeader.tsx"
      provides: "Fixed header with branding and sort/filter icon buttons"
      exports: ["default"]
    - path: "packages/frontend/src/components/snap/SortSheet.tsx"
      provides: "Sort bottom sheet with 5 sort options and swipe-to-dismiss"
      exports: ["default"]
    - path: "packages/frontend/src/App.css"
      provides: "CSS for .fixed-header, .sort-sheet-backdrop, .sort-sheet, .header-icon-btn, .header-icon-dot"
      contains: ".fixed-header"
  key_links:
    - from: "packages/frontend/src/components/snap/SortSheet.tsx"
      to: "useFeedState types"
      via: "import type { FeedState, FeedAction, SortMode }"
      pattern: "import.*FeedState.*FeedAction"
    - from: "packages/frontend/src/components/snap/FixedHeader.tsx"
      to: "useFeedState types"
      via: "import type { FeedState }"
      pattern: "import.*FeedState"
---

<objective>
Create the FixedHeader and SortSheet components with their CSS styles.

Purpose: Build the two new UI components that replace the old SnapControlBar -- a permanently visible header with branding and icon buttons, and a sort bottom sheet that mirrors the existing FilterSheet design.

Output: Two new component files and CSS additions to App.css.
</objective>

<execution_context>
@/home/kylesulit/.claude/get-shit-done/workflows/execute-plan.md
@/home/kylesulit/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-fixed-header-sort-bottom-sheet/13-RESEARCH.md
@packages/frontend/src/components/snap/FilterSheet.tsx
@packages/frontend/src/hooks/useFeedState.ts
@packages/frontend/src/App.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create FixedHeader and SortSheet components</name>
  <files>
    packages/frontend/src/components/snap/FixedHeader.tsx
    packages/frontend/src/components/snap/SortSheet.tsx
  </files>
  <action>
Create two new component files:

**FixedHeader.tsx:**
- Props: `{ feedState: FeedState; onSortClick: () => void; onFilterClick: () => void }`
- Import `FeedState` type from `../../hooks/useFeedState`
- Render a `<header className="fixed-header">` with:
  - Left: `<span className="fixed-header-brand">Army Feed</span>` (bold white text, no icon/logo per user decision)
  - Right: `<div className="fixed-header-actions">` containing two `<button className="header-icon-btn">` elements
  - Sort button: inline SVG icon (horizontal lines decreasing in width: paths "M3 6h18", "M6 12h12", "M9 18h6"), calls `onSortClick`, has `aria-label="Sort"`
  - When `feedState.sort !== "recommended"`, show `<span className="header-icon-dot" />` inside the sort button (small colored dot indicator per user decision)
  - Filter button: inline SVG icon (funnel: lines x1=4/y1=6/x2=20/y2=6, x1=6/y1=12/x2=18/y2=12, x1=8/y1=18/x2=16/y2=18), calls `onFilterClick`, has `aria-label="Filter"`
  - When `filterCount > 0` (sum of sources.length + members.length + contentTypes.length), show `<span className="filter-badge">{filterCount}</span>` on the filter button
- Default export the component

**SortSheet.tsx:**
- Clone the FilterSheet.tsx portal/backdrop/swipe-to-dismiss pattern exactly
- Props: `{ isOpen: boolean; onClose: () => void; feedState: FeedState; dispatch: React.Dispatch<FeedAction> }`
- Import `FeedState`, `FeedAction`, `SortMode` from `../../hooks/useFeedState`
- Import `createPortal` from `react-dom`, `useState`, `useEffect`, `useRef` from `react`
- Define `SORT_OPTIONS` array at module level: `{ mode: SortMode; label: string }[]` with all 5 modes: recommended="Recommended", newest="Newest First", oldest="Oldest First", popular="Most Popular", discussed="Most Discussed"
- Body scroll lock: same `useEffect` as FilterSheet (set `document.body.style.overflow = "hidden"` when open, restore on cleanup)
- Touch handling: copy FilterSheet's `sheetRef`, `touchStartY`, `currentTranslateY`, `scrollTopRef` refs and `handleTouchStart`/`handleTouchMove`/`handleTouchEnd` functions identically (80px threshold, cubic-bezier easing)
- Render via `createPortal(jsx, document.body)`:
  - Outer: `<div className="sort-sheet-backdrop${isOpen ? ' open' : ''}" onClick={onClose}>`
  - Inner: `<div ref={sheetRef} className="sort-sheet" onClick={e => e.stopPropagation()} onTouchStart/Move/End={handlers}>`
  - Handle bar: `<div className="sort-sheet-handle" />`
  - Title: `<h3 className="sort-sheet-title">Sort By</h3>` (per user decision: "Sort By" title matching filter sheet header pattern)
  - Options list: map `SORT_OPTIONS` to `<button className="sort-sheet-option${active ? ' active' : ''}" onClick={() => dispatch({ type: "SET_SORT", sort: opt.mode })}>`
  - Each option: `<span>{opt.label}</span>` and when active `feedState.sort === opt.mode`, show a checkmark SVG (polyline points "20 6 9 17 4 12") per user decision
  - Tapping an option applies immediately (dispatches SET_SORT), does NOT auto-close the sheet (per user decision)
- Default export the component
  </action>
  <verify>
Run `npx tsc --noEmit` from packages/frontend -- both files should compile without type errors. Verify FixedHeader.tsx and SortSheet.tsx exist in packages/frontend/src/components/snap/.
  </verify>
  <done>
FixedHeader.tsx exports a default component with branding + sort/filter icon buttons. SortSheet.tsx exports a default component with portal-based bottom sheet, 5 sort options, checkmark on active, swipe-to-dismiss. Both compile without errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add CSS styles for FixedHeader and SortSheet</name>
  <files>
    packages/frontend/src/App.css
  </files>
  <action>
Add CSS styles to the end of App.css (before any closing comments, after the existing filter-sheet styles section). Add two new sections:

**Fixed Header styles (`/* === Fixed Header === */`):**
- `.fixed-header`: `flex-shrink: 0; display: flex; align-items: center; justify-content: space-between; height: calc(44px + env(safe-area-inset-top)); padding-top: env(safe-area-inset-top); padding-left: 16px; padding-right: 8px; background: var(--bg-card, #1a1a2e); border-bottom: 1px solid rgba(255, 255, 255, 0.1); z-index: 20;`
  - The `env(safe-area-inset-top)` handles iOS notch/dynamic island per PWA best practices (Claude's discretion)
  - `flex-shrink: 0` ensures the header doesn't compress when the feed fills remaining space
- `.fixed-header-brand`: `font-size: 17px; font-weight: 700; color: var(--text-primary); letter-spacing: 0.02em;`
- `.fixed-header-actions`: `display: flex; align-items: center; gap: 4px;`
- `.header-icon-btn`: `position: relative; background: transparent; border: none; color: rgba(255, 255, 255, 0.7); padding: 8px; cursor: pointer; -webkit-tap-highlight-color: transparent;`
- `.header-icon-dot`: `position: absolute; top: 6px; right: 6px; width: 6px; height: 6px; border-radius: 50%; background: var(--theme-primary);`

**Sort Sheet styles (`/* === Sort Sheet === */`):**
Mirror the existing filter-sheet CSS pattern exactly (same backdrop, same animation, same border-radius/handle/padding):
- `.sort-sheet-backdrop`: identical to `.filter-sheet-backdrop` -- `position: fixed; inset: 0; background: rgba(0, 0, 0, 0.6); z-index: 200; display: flex; align-items: flex-end; opacity: 0; pointer-events: none; transition: opacity 0.3s;`
- `.sort-sheet-backdrop.open`: `opacity: 1; pointer-events: auto;`
- `.sort-sheet`: identical layout to `.filter-sheet` -- `width: 100%; background: var(--bg-card, #1a1a2e); border-radius: 16px 16px 0 0; padding: 0 16px 24px; padding-bottom: max(24px, env(safe-area-inset-bottom)); transform: translateY(100%); transition: transform 0.3s cubic-bezier(0.32, 0.72, 0, 1); z-index: 201;`
- `.sort-sheet-backdrop.open .sort-sheet`: `transform: translateY(0);`
- `.sort-sheet-handle`: identical to `.filter-sheet-handle` -- centered pill handle at top for drag affordance
- `.sort-sheet-title`: `font-size: 16px; font-weight: 600; color: var(--text-primary); margin: 8px 0 16px; text-align: center;`
- `.sort-sheet-option`: `display: flex; align-items: center; justify-content: space-between; width: 100%; padding: 14px 8px; background: transparent; border: none; border-bottom: 1px solid rgba(255, 255, 255, 0.06); color: rgba(255, 255, 255, 0.7); font-size: 15px; cursor: pointer; -webkit-tap-highlight-color: transparent;`
- `.sort-sheet-option.active`: `color: var(--theme-primary); font-weight: 600;`
- `.sort-sheet-option:last-child`: `border-bottom: none;`

Also add `prefers-reduced-motion` media query overrides for the sort sheet transition (matching the existing filter sheet reduced motion handling if present).

Look at the existing filter-sheet handle CSS in App.css and match it exactly for sort-sheet-handle.
  </action>
  <verify>
Run `npm run build` from packages/frontend (or the root workspace build command). Build should succeed with no CSS errors. Visually inspect App.css to confirm both sections were added and the sort-sheet styles mirror the filter-sheet styles.
  </verify>
  <done>
App.css contains .fixed-header and .sort-sheet CSS sections. Sort sheet styles are visually identical to filter sheet styles (same backdrop, animation easing, border-radius, handle, safe-area padding). Header uses flex-shrink: 0 with safe-area-inset-top padding for iOS PWA.
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes from packages/frontend -- new components compile cleanly
2. FixedHeader.tsx has "Army Feed" text, sort icon with dot indicator, filter icon with badge count
3. SortSheet.tsx has createPortal, backdrop, 5 sort options, checkmark SVG, swipe-to-dismiss touch handlers
4. App.css has .fixed-header section with env(safe-area-inset-top) and .sort-sheet section mirroring .filter-sheet
</verification>

<success_criteria>
- Two new component files exist and compile
- CSS styles are added matching the dark theme and FilterSheet design language
- Components are ready to be wired into News.tsx in the next plan
</success_criteria>

<output>
After completion, create `.planning/phases/13-fixed-header-sort-bottom-sheet/13-01-SUMMARY.md`
</output>
