---
phase: 13-fixed-header-sort-bottom-sheet
plan: 02
type: execute
wave: 2
depends_on:
  - 13-01
files_modified:
  - packages/frontend/src/pages/News.tsx
  - packages/frontend/src/components/snap/SnapFeed.tsx
  - packages/frontend/src/components/snap/SnapControlBar.tsx
  - packages/frontend/src/hooks/useControlBarVisibility.ts
  - packages/frontend/src/App.css
autonomous: false
requirements:
  - NAV-01
  - NAV-02
  - NAV-03
  - NAV-04
  - NAV-05

must_haves:
  truths:
    - "Army Feed branding is always visible at the top of every card in the feed without tapping or scrolling"
    - "Sort and Filter buttons in the header open their respective bottom sheets"
    - "Selecting a sort option in the Sort sheet reorders the feed (all 5 modes work)"
    - "The old auto-hide control bar, snap-reveal-zone, and useControlBarVisibility code are gone"
    - "Vertical swipe navigation between cards still works correctly with the header in place"
  artifacts:
    - path: "packages/frontend/src/pages/News.tsx"
      provides: "Snap mode rendering with FixedHeader + SortSheet replacing SnapControlBar"
      contains: "FixedHeader"
    - path: "packages/frontend/src/components/snap/SnapControlBar.tsx"
      provides: "DELETED"
    - path: "packages/frontend/src/hooks/useControlBarVisibility.ts"
      provides: "DELETED"
  key_links:
    - from: "packages/frontend/src/pages/News.tsx"
      to: "packages/frontend/src/components/snap/FixedHeader.tsx"
      via: "import and render in snap mode JSX"
      pattern: "import FixedHeader"
    - from: "packages/frontend/src/pages/News.tsx"
      to: "packages/frontend/src/components/snap/SortSheet.tsx"
      via: "import and render with isSortOpen state"
      pattern: "import SortSheet"
    - from: "packages/frontend/src/pages/News.tsx"
      to: "packages/frontend/src/components/snap/SnapFeed.tsx"
      via: "pagingDisabled prop includes isSortOpen"
      pattern: "pagingDisabled=.*isSortOpen"
---

<objective>
Wire FixedHeader and SortSheet into News.tsx, delete old control bar code, and verify the complete integration.

Purpose: Replace the auto-hiding overlay control bar with the new always-visible header and sort bottom sheet. Remove all dead code (SnapControlBar, useControlBarVisibility, snap-reveal-zone). Ensure feed paging still works correctly with the header in place.

Output: Updated News.tsx with new components, deleted old files, cleaned CSS.
</objective>

<execution_context>
@/home/kylesulit/.claude/get-shit-done/workflows/execute-plan.md
@/home/kylesulit/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-fixed-header-sort-bottom-sheet/13-RESEARCH.md
@.planning/phases/13-fixed-header-sort-bottom-sheet/13-01-SUMMARY.md
@packages/frontend/src/pages/News.tsx
@packages/frontend/src/components/snap/SnapFeed.tsx
@packages/frontend/src/components/snap/SnapControlBar.tsx
@packages/frontend/src/hooks/useControlBarVisibility.ts
@packages/frontend/src/App.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire FixedHeader and SortSheet into News.tsx and delete old control bar code</name>
  <files>
    packages/frontend/src/pages/News.tsx
    packages/frontend/src/components/snap/SnapControlBar.tsx
    packages/frontend/src/hooks/useControlBarVisibility.ts
    packages/frontend/src/components/snap/SnapFeed.tsx
    packages/frontend/src/App.css
  </files>
  <action>
**News.tsx changes:**

1. REMOVE these imports:
   - `import SnapControlBar from "../components/snap/SnapControlBar";`
   - `import { useControlBarVisibility } from "../hooks/useControlBarVisibility";`

2. ADD these imports:
   - `import FixedHeader from "../components/snap/FixedHeader";`
   - `import SortSheet from "../components/snap/SortSheet";`

3. ADD state for sort sheet:
   - `const [isSortOpen, setIsSortOpen] = useState(false);`

4. REMOVE the `snapIndex` state and `useControlBarVisibility` hook call:
   - Delete: `const [snapIndex, setSnapIndex] = useState(0);`
   - Delete: `const { visible: barVisible, showBar } = useControlBarVisibility({ currentIndex: snapIndex });`

5. KEEP the card-position-preservation logic (prevItemsRef, currentItemIdRef, startIndex useMemo, startIndex useEffect) but adapt it:
   - The `snapIndex` state was used for both control bar visibility AND card position preservation. Since we're removing `snapIndex`, we need an alternative. The `onIndexChange` callback on SnapFeed currently sets `snapIndex`. We still need to track the current index for position preservation.
   - Keep `const [snapIndex, setSnapIndex] = useState(0);` BUT remove the `useControlBarVisibility` line. The `snapIndex` is still needed for `currentItemIdRef` tracking in the card-position-preservation logic.
   - So: keep `snapIndex` and `setSnapIndex`, remove only `useControlBarVisibility` import and usage.

6. UPDATE the snap mode return JSX:
   - Replace `<SnapControlBar ... />` and the `snap-reveal-zone` div with `<FixedHeader feedState={feedState} onSortClick={() => setIsSortOpen(true)} onFilterClick={() => setIsFilterOpen(true)} />`
   - The FixedHeader must be a direct child of `.snap-page` div, rendered BEFORE the loading/feed content (so flexbox gives it fixed space at top)
   - Update SnapFeed `pagingDisabled` prop: `pagingDisabled={isFilterOpen || isSortOpen}` (add `isSortOpen` to prevent paging while sort sheet is open, per research pitfall #5)
   - Add `<SortSheet isOpen={isSortOpen} onClose={() => setIsSortOpen(false)} feedState={feedState} dispatch={dispatch} />` alongside the existing FilterSheet
   - Final snap mode JSX structure:
     ```
     <div className="snap-page">
       <FixedHeader ... />
       {loading ? <SnapSkeleton /> : <SnapFeed ... pagingDisabled={isFilterOpen || isSortOpen} />}
       <SortSheet ... />
       <FilterSheet ... />
     </div>
     ```

7. REMOVE the `snap-reveal-zone` div and conditional: delete the `{!barVisible && (<div className="snap-reveal-zone" onClick={showBar} />)}` block entirely

**Delete old files:**
- Delete `packages/frontend/src/components/snap/SnapControlBar.tsx` entirely (using `rm` or equivalent)
- Delete `packages/frontend/src/hooks/useControlBarVisibility.ts` entirely

**SnapFeed.tsx changes:**
- The `onIndexChange` prop and the useEffect that calls it can stay -- it's still used for position preservation. No changes needed to SnapFeed.tsx itself if `onIndexChange` is still passed from News.tsx.

**App.css cleanup:**
- Remove the entire `.snap-control-bar` CSS section (`.snap-control-bar`, `.snap-control-bar-initial`, `.snap-control-bar.hidden`, `.snap-control-bar-row`, `.sort-tabs`, `.sort-tab`, `.sort-tab.active`, `.filter-icon-btn`, `.filter-badge`, `.active-filter-chips`, `.filter-chip`, `.filter-clear-all`)
- Remove `.snap-reveal-zone` CSS rule
- Keep the `.snap-page` rule but ensure it's `display: flex; flex-direction: column` (it should already be, since it uses `position: fixed; inset: 0` and needs flexbox for header + feed layout). If `.snap-page` doesn't already have `display: flex; flex-direction: column`, add it.
- The `.snap-feed` should have `flex: 1; overflow: hidden;` (or `min-height: 0; flex: 1`) so it fills remaining viewport after the header. Check if this is already the case; if not, add `flex: 1` to `.snap-feed`.

Verify that no other file imports SnapControlBar or useControlBarVisibility before deleting. Use grep to check:
```
grep -r "SnapControlBar\|useControlBarVisibility" packages/frontend/src/ --include="*.tsx" --include="*.ts"
```
Only News.tsx should reference them. If any other file does, update it too.
  </action>
  <verify>
1. `npx tsc --noEmit` from packages/frontend passes (no broken imports, no type errors)
2. `npm run build` from project root succeeds (production build works)
3. `grep -r "SnapControlBar\|useControlBarVisibility\|snap-reveal-zone" packages/frontend/src/` returns zero results
4. SnapControlBar.tsx and useControlBarVisibility.ts files no longer exist on disk
5. News.tsx snap mode JSX contains FixedHeader, SortSheet, and pagingDisabled includes isSortOpen
  </verify>
  <done>
News.tsx snap mode renders FixedHeader and SortSheet instead of SnapControlBar. Old files deleted. Build passes. No dead code references remain.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify fixed header and sort sheet visually</name>
  <files>packages/frontend/src/pages/News.tsx</files>
  <action>
Deploy the app for visual verification. What was built: Fixed header with "Army Feed" branding and sort/filter icon buttons, replacing the old auto-hide control bar. Sort bottom sheet with 5 sort options matching the filter sheet's design. Old control bar, reveal zone, and visibility hook deleted.

Verification steps:
1. Open the app in a browser (or deploy via /share)
2. Verify "Army Feed" text is always visible at the top-left of the screen on every card
3. Verify sort and filter icon buttons are visible at top-right
4. Tap the sort icon -- Sort By sheet should slide up with 5 options: Recommended, Newest First, Oldest First, Most Popular, Most Discussed
5. Tap "Newest First" -- checkmark should appear next to it, feed should reorder. Sheet should stay open (not auto-close).
6. Swipe down on the sort sheet or tap outside to close it
7. Verify a small colored dot appears on the sort icon (since non-default sort is active)
8. Tap the filter icon -- Filter sheet should slide up with the same animation style
9. Swipe vertically on the feed to navigate between cards -- paging should still work correctly
10. With a bottom sheet open, swiping should NOT page the feed
11. Verify no invisible tap targets or old control bar elements remain (no hidden tap zone at top)
  </action>
  <verify>User confirms all 11 verification steps pass. Type "approved" or describe issues.</verify>
  <done>Human verified: header always visible, sort sheet works with all 5 options, filter sheet still works, paging works correctly, no old control bar remnants.</done>
</task>

</tasks>

<verification>
1. TypeScript compilation passes with zero errors
2. Production build succeeds
3. No references to SnapControlBar, useControlBarVisibility, or snap-reveal-zone remain in codebase
4. "Army Feed" header is always visible without tapping or scrolling
5. Sort sheet opens via header icon, shows 5 options, applies immediately on tap
6. Filter sheet still works via header icon
7. Sort and filter sheets share identical slide-up animation and visual design
8. Feed paging works correctly with header in place (cards fill viewport minus header height)
9. Paging is disabled when either sheet is open
</verification>

<success_criteria>
- The old auto-hide control bar is completely gone (files deleted, CSS removed, no dead imports)
- FixedHeader with branding and icon buttons is always visible
- SortSheet opens from header, has 5 options, applies instantly, shows checkmark on active
- Both sheets share the same slide-up design language
- Feed paging still works with the header taking fixed space at the top
- Human verification confirms correct visual appearance and interaction
</success_criteria>

<output>
After completion, create `.planning/phases/13-fixed-header-sort-bottom-sheet/13-02-SUMMARY.md`
</output>
