---
phase: 02-feed-expansion
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/config/groups/bts/sources.ts
autonomous: true
gap_closure: true
requirements:
  - SRC-02

must_haves:
  truths:
    - "At least 2 fan YouTube channel entries in BTS config have verified channel IDs and are enabled (no enabled:false flag)"
    - "No placeholder or stub URLs remain in YouTube source entries"
    - "Fan YouTube content (beyond official BANGTANTV/HYBE) reaches users when the app loads"
  artifacts:
    - path: "src/config/groups/bts/sources.ts"
      provides: "Fan YouTube source entries with verified channel IDs"
      contains: "UC5m4L0y_OJIJ2NWPRcayXvg"
  key_links:
    - from: "src/config/groups/bts/sources.ts"
      to: "src/services/sources/youtube.ts"
      via: "type: 'youtube' entries matched by registry"
      pattern: "type: \"youtube\""
---

<objective>
Fix blocked SRC-02 requirement: replace unverified/placeholder fan YouTube channel IDs with verified IDs and enable the entries so fan YouTube content actually reaches users.

Purpose: Phase 2 verification found that both fan YouTube entries (yt-bangtansubs, yt-bts-fan-edits) have enabled:false, and one uses a literal placeholder URL. SRC-02 ("User can see content from fan YouTube channels beyond official BANGTANTV/HYBE") cannot be satisfied.

Output: Updated src/config/groups/bts/sources.ts with verified fan YouTube channel entries enabled by default.
</objective>

<execution_context>
@/home/kylesulit/.claude/get-shit-done/workflows/execute-plan.md
@/home/kylesulit/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-feed-expansion/02-VERIFICATION.md
@.planning/phases/02-feed-expansion/02-01-SUMMARY.md
@src/config/groups/bts/sources.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace fan YouTube entries with verified channel IDs and enable them</name>
  <files>src/config/groups/bts/sources.ts</files>
  <action>
    Update the two fan YouTube channel entries in the sources array:

    1. **yt-bangtansubs**: Replace the unverified channel ID `UCfBiWbcbqMGBnfBLcsZFDeA` with the verified ID `UC5m4L0y_OJIJ2NWPRcayXvg` (confirmed via NoxInfluencer: https://www.noxinfluencer.com/youtube/channel/UC5m4L0y_OJIJ2NWPRcayXvg). This is the BangtanSubs fan translation channel with 1.5M subscribers -- all content is BTS, so needsFilter remains false. Remove the `enabled: false` line and the comment about needing verification.

    2. **yt-bts-fan-edits**: Replace entirely. Change id to `yt-dkdktv`, label to `DKDKTV`, url to `UCVEzR8VHu0JC5xlTr53cMwQ` (confirmed via vidIQ: https://vidiq.com/youtube-stats/channel/UCVEzR8VHu0JC5xlTr53cMwQ/ and SocialBlade: https://socialblade.com/youtube/channel/UCVEzR8VHu0JC5xlTr53cMwQ). This is a Korean culture/K-pop commentary channel -- set needsFilter: true since they cover more than just BTS. Remove the `enabled: false` line and the comment about needing verification.

    The resulting entries should look like:

    ```typescript
    // Fan YouTube channels
    {
      type: "youtube",
      id: "yt-bangtansubs",
      label: "BANGTANSUBS",
      url: "UC5m4L0y_OJIJ2NWPRcayXvg",
      needsFilter: false,
      fetchCount: 10,
      priority: 12,
    },
    {
      type: "youtube",
      id: "yt-dkdktv",
      label: "DKDKTV",
      url: "UCVEzR8VHu0JC5xlTr53cMwQ",
      needsFilter: true,
      fetchCount: 10,
      priority: 13,
    },
    ```

    Do NOT change any other source entries. Only modify the two fan YouTube channel entries (lines ~103-122).
  </action>
  <verify>
    1. Run `grep -c 'enabled: false' src/config/groups/bts/sources.ts` -- should return 0 (no disabled entries remain)
    2. Run `grep -c 'UCplaceholder' src/config/groups/bts/sources.ts` -- should return 0 (no placeholder URLs)
    3. Run `grep 'UC5m4L0y_OJIJ2NWPRcayXvg' src/config/groups/bts/sources.ts` -- should match (BangtanSubs ID present)
    4. Run `grep 'UCVEzR8VHu0JC5xlTr53cMwQ' src/config/groups/bts/sources.ts` -- should match (DKDKTV ID present)
    5. Run `npx tsc --noEmit` -- no type errors
    6. Run `npx vite build` -- builds successfully
  </verify>
  <done>
    Two fan YouTube channel entries have verified channel IDs, no enabled:false flag, no placeholder URLs. The app will fetch fan YouTube content (BangtanSubs translations, DKDKTV K-pop commentary filtered for BTS keywords) alongside official BANGTANTV/HYBE content. SRC-02 is satisfied.
  </done>
</task>

</tasks>

<verification>
- `src/config/groups/bts/sources.ts` contains 4 YouTube entries total: 2 official (BANGTANTV, HYBE) + 2 fan (BANGTANSUBS, DKDKTV)
- No YouTube entry has `enabled: false`
- No entry has a placeholder URL
- TypeScript compiles without errors
- Vite build succeeds
- Fan YouTube entries use verified channel IDs that resolve to real YouTube Atom feeds at `https://www.youtube.com/feeds/videos.xml?channel_id={ID}`
</verification>

<success_criteria>
SRC-02 unblocked: fan YouTube content from BangtanSubs and DKDKTV reaches users in the feed alongside official channels.
</success_criteria>

<output>
After completion, create `.planning/phases/02-feed-expansion/02-03-SUMMARY.md`
</output>
