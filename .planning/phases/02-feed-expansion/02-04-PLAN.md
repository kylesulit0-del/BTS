---
phase: 02-feed-expansion
plan: 04
type: execute
wave: 1
depends_on: []
files_modified: [src/services/feeds.ts, src/config/groups/bts/sources.ts]
autonomous: true
gap_closure: true
requirements: [SRC-02, STAT-01, STAT-02, STAT-03]

must_haves:
  truths:
    - "YouTube videos up to 30 days old appear in the feed (BANGTANTV's 15-day-old videos are no longer filtered out)"
    - "HYBE LABELS channel returns valid content (HTTP 200 from YouTube Atom feed)"
    - "YouTube feed cards display view counts and like counts with icons when stats are above threshold"
    - "Feed contains videos from at least 3 YouTube sources (BANGTANTV, HYBE LABELS, DKDKTV)"
  artifacts:
    - path: "src/services/feeds.ts"
      provides: "Increased MAX_AGE_MS allowing YouTube content through"
      contains: "30 * 24 * 60 * 60 * 1000"
    - path: "src/config/groups/bts/sources.ts"
      provides: "Corrected HYBE LABELS channel ID"
      contains: "UC3IZKseVpdzPSBaWxBxundA"
  key_links:
    - from: "src/config/groups/bts/sources.ts"
      to: "src/services/sources/youtube.ts"
      via: "HYBE channel ID used to construct YouTube Atom feed URL"
      pattern: "UC3IZKseVpdzPSBaWxBxundA"
    - from: "src/services/feeds.ts"
      to: "src/services/feeds.ts:applyFeedPipeline"
      via: "MAX_AGE_MS constant used in age filter"
      pattern: "MAX_AGE_MS"
---

<objective>
Fix the two code-level root causes blocking YouTube content and stats from appearing in the feed: (1) increase MAX_AGE_MS from 7 days to 30 days so YouTube videos with real engagement survive the age filter, and (2) fix the wrong HYBE LABELS channel ID that returns 404.

Purpose: UAT tests 4 and 6 failed because the 7-day age filter eliminates all BANGTANTV videos (newest is 15 days old, with 544K views) and the wrong HYBE channel ID returns zero videos. These two fixes immediately unblock YouTube content with visible engagement stats.

Output: Updated feeds.ts with 30-day age window, updated sources.ts with correct HYBE channel ID.
</objective>

<execution_context>
@/home/kylesulit/.claude/get-shit-done/workflows/execute-plan.md
@/home/kylesulit/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/02-feed-expansion/02-UAT.md
@.planning/debug/youtube-only-one-video.md
@.planning/debug/youtube-stats-missing.md
@src/services/feeds.ts
@src/config/groups/bts/sources.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Increase MAX_AGE_MS and fix HYBE channel ID</name>
  <files>src/services/feeds.ts, src/config/groups/bts/sources.ts</files>
  <action>
In `src/services/feeds.ts` line 7, change `MAX_AGE_MS` from `7 * 24 * 60 * 60 * 1000` (7 days) to `30 * 24 * 60 * 60 * 1000` (30 days). This allows YouTube videos with 2-3 week posting cadences to survive the age filter. BANGTANTV's newest video (15 days old, 544K views, 106K likes) will now appear in the feed with visible stats.

In `src/config/groups/bts/sources.ts` line 56, change the HYBE LABELS channel ID from `UCx2hOXK_cGnRolCRilNUfA` (returns 404) to `UC3IZKseVpdzPSBaWxBxundA` (verified correct ID, 79M subscribers). This was confirmed via SocialBlade/NoxInfluencer and curl testing during the debug session.

Do NOT change any other code. The stats extraction pipeline (xmlParser.ts, youtube.ts, FeedCard.tsx) is verified correct end-to-end -- the problem was purely that high-stat videos were filtered out before reaching the renderer.
  </action>
  <verify>
1. Run `npx tsc --noEmit` -- should compile without errors
2. Run `grep "30 \* 24" src/services/feeds.ts` -- should show the new MAX_AGE_MS
3. Run `grep "UC3IZKseVpdzPSBaWxBxundA" src/config/groups/bts/sources.ts` -- should show HYBE entry
4. Run `curl -s -o /dev/null -w "%{http_code}" "https://www.youtube.com/feeds/videos.xml?channel_id=UC3IZKseVpdzPSBaWxBxundA"` -- should return 200
5. Verify no other changes: `git diff --stat` should show exactly 2 files changed
  </verify>
  <done>
MAX_AGE_MS is 30 days (allowing YouTube videos up to 30 days old into the feed). HYBE LABELS channel ID is UC3IZKseVpdzPSBaWxBxundA (returns HTTP 200 from YouTube Atom feed). TypeScript compiles cleanly. Only feeds.ts and sources.ts were modified.
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- HYBE channel ID responds with 200 (not 404)
- MAX_AGE_MS constant is 30 days
- No regressions: engagement scoring formula unchanged, deduplication unchanged, per-source cap unchanged
</verification>

<success_criteria>
- BANGTANTV videos (15+ days old with 544K views) would now survive the age filter and display stats
- HYBE LABELS feed resolves with valid Atom XML containing videos
- All other feed pipeline behavior (scoring, dedup, capping) unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/02-feed-expansion/02-04-SUMMARY.md`
</output>
