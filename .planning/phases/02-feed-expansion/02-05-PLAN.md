---
phase: 02-feed-expansion
plan: 05
type: execute
wave: 1
depends_on: [02-04]
files_modified: [src/config/groups/bts/sources.ts]
autonomous: true
gap_closure: true
requirements: [SRC-02, SRC-03, SRC-04]

must_haves:
  truths:
    - "Tumblr source entries point to actively-posting BTS fan blogs (posts within the last 30 days)"
    - "Tumblr filter chip shows Tumblr content when clicked (not 'no content available')"
    - "Inactive BangtanSubs YouTube channel is replaced with an active fan channel"
    - "Feed contains content from at least 4 distinct YouTube sources"
  artifacts:
    - path: "src/config/groups/bts/sources.ts"
      provides: "Active Tumblr blog URLs and replacement fan YouTube channel ID"
  key_links:
    - from: "src/config/groups/bts/sources.ts"
      to: "src/services/sources/tumblr.ts"
      via: "Tumblr RSS URLs fetched by tumblr fetcher"
      pattern: "tumblr\\.com/rss"
    - from: "src/config/groups/bts/sources.ts"
      to: "src/services/sources/youtube.ts"
      via: "YouTube channel IDs used to construct Atom feed URLs"
      pattern: "type: \"youtube\""
---

<objective>
Replace all 5 inactive Tumblr blog entries with actively-posting BTS fan blogs, and replace the inactive BangtanSubs YouTube channel with an active fan channel. This is source curation to close UAT gaps where Tumblr content was absent and YouTube variety was insufficient.

Purpose: UAT tests 1 and 2 failed because all 5 configured Tumblr blogs stopped posting (last post Nov 2025 at best, May 2021 at worst). UAT test 6 showed only 1 YouTube video partly because BangtanSubs hasn't posted in ~8 months. Replacing with active sources ensures users see Tumblr content and diverse YouTube content.

Output: Updated sources.ts with verified, active Tumblr blog RSS URLs and a replacement fan YouTube channel.
</objective>

<execution_context>
@/home/kylesulit/.claude/get-shit-done/workflows/execute-plan.md
@/home/kylesulit/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/02-feed-expansion/02-UAT.md
@.planning/debug/thumbnails-not-loading.md
@.planning/debug/youtube-only-one-video.md
@src/config/groups/bts/sources.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Research and verify active BTS Tumblr blogs</name>
  <files>src/config/groups/bts/sources.ts</files>
  <action>
Research actively-posting BTS fan blogs on Tumblr. For each candidate blog:

1. Fetch `https://{blogname}.tumblr.com/rss` via curl to confirm the RSS feed returns HTTP 200
2. Check the `<pubDate>` of the most recent `<item>` to verify the blog posted within the last 30 days
3. Verify content is BTS-related (not a general K-pop blog that moved on to other groups)

**Quality criteria for inclusion:**
- Must have posted within the last 30 days
- Must be primarily BTS content (needsFilter: false for BTS-dedicated blogs, needsFilter: true for broader K-pop blogs)
- RSS feed must be accessible (HTTP 200, valid RSS XML)

**Search strategy:**
- Try well-known BTS Tumblr accounts: bts-trans, eternally-namjoon, rmarchives, btsarmyaesthetics, bangtanpurplehearts, bts7updates, btsenthusiast, bangtan-theory, bts-daily, jhopesangel, btsgfx, rapline-network
- Use web search for "active BTS Tumblr blogs 2025 2026" to find currently posting accounts
- Need exactly 5 active blogs to replace the 5 inactive ones

**Replace all 5 Tumblr entries** in `src/config/groups/bts/sources.ts` (lines 133-178). Keep the same structure (type, id pattern, fetchCount, priority numbers). Update `label`, `url`, and `needsFilter` as needed. Preserve the `id` prefix pattern: `tumblr-{blogname}`.

If fewer than 5 active BTS blogs can be found, reduce the count rather than including inactive ones. Document which blogs were verified and their last post date in the commit message.
  </action>
  <verify>
1. For each new Tumblr URL in sources.ts, run: `curl -s -o /dev/null -w "%{http_code}" "{url}"` -- all should return 200
2. Run `npx tsc --noEmit` -- should compile without errors
3. Verify no entries point to the old inactive blogs: `grep -c "btsfandom\|allforbts\|0613data\|beautifulpersonpeach" src/config/groups/bts/sources.ts` should return 0 (bangtan.tumblr.com is the official Tumblr and could be retained if it's active)
4. Count Tumblr entries: `grep -c 'type: "tumblr"' src/config/groups/bts/sources.ts` should be 3-5
  </verify>
  <done>
All Tumblr source entries point to blogs that have posted within the last 30 days. Each blog's RSS feed returns HTTP 200. Old inactive blogs (btsfandom, allforbts, 0613data, beautifulpersonpeach, and bangtan if inactive) are removed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace inactive BangtanSubs with active fan YouTube channel</name>
  <files>src/config/groups/bts/sources.ts</files>
  <action>
Replace the BangtanSubs YouTube entry (id: `yt-bangtansubs`, channel ID `UC5m4L0y_OJIJ2NWPRcayXvg`, last post July 2024) with an active BTS fan YouTube channel.

**Research approach:**
- Search for active BTS fan YouTube channels: reaction channels, edit/compilation channels, news/update channels, lyric/translation channels
- Verify each candidate by fetching `https://www.youtube.com/feeds/videos.xml?channel_id={ID}` and checking the most recent `<published>` date
- Channel must have posted within the last 30 days
- Channel must have >50K subscribers (quality threshold from research phase)

**Candidates to try** (from research and known BTS YouTube ecosystem):
- BTS Moments / BTS TikTok compilations channels
- K-pop fan reaction channels that regularly cover BTS (would need needsFilter: true)
- BTS update/news channels

**Update the entry** in sources.ts:
- Change `id` to match the new channel (e.g., `yt-{channelname}`)
- Change `label` to the channel name
- Change `url` to the verified channel ID (UC... format)
- Set `needsFilter: true` if channel covers content beyond BTS
- Keep `fetchCount: 10` and `priority: 12`

Verify the replacement channel ID returns HTTP 200 from YouTube's Atom feed endpoint before committing.
  </action>
  <verify>
1. Run `curl -s -o /dev/null -w "%{http_code}" "https://www.youtube.com/feeds/videos.xml?channel_id={new_id}"` -- should return 200
2. Run `npx tsc --noEmit` -- should compile without errors
3. Verify BangtanSubs is gone: `grep -c "UC5m4L0y_OJIJ2NWPRcayXvg" src/config/groups/bts/sources.ts` should return 0
4. Verify 4 YouTube entries total: `grep -c 'type: "youtube"' src/config/groups/bts/sources.ts` should be 4
5. Check newest video age by parsing the Atom feed: newest `<published>` should be within 30 days
  </verify>
  <done>
BangtanSubs replaced with an active fan YouTube channel that has posted within the last 30 days. Channel ID verified (HTTP 200 from Atom feed). Total YouTube source count remains at 4 (2 official + 2 fan).
  </done>
</task>

</tasks>

<verification>
- All Tumblr RSS URLs return HTTP 200 and have content within 30 days
- Replacement YouTube channel ID returns HTTP 200 with recent content
- `npx tsc --noEmit` passes
- No inactive sources remain (btsfandom, allforbts, 0613data, beautifulpersonpeach, BangtanSubs all replaced)
- Source entry structure preserved (type, id, label, url, needsFilter, fetchCount, priority)
</verification>

<success_criteria>
- Tumblr filter chip would show actual content from active blogs
- YouTube feed would show content from 4 active sources (BANGTANTV, HYBE, DKDKTV, new fan channel)
- All source RSS/Atom feeds return HTTP 200
- Zero inactive sources in the config
</success_criteria>

<output>
After completion, create `.planning/phases/02-feed-expansion/02-05-SUMMARY.md`
</output>
