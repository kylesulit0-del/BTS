---
phase: 07-llm-moderation-pipeline
plan: 03
type: execute
wave: 3
depends_on: ["07-02"]
files_modified:
  - packages/frontend/src/components/FeedCard.tsx
  - packages/frontend/src/pages/News.tsx
  - packages/frontend/src/pages/Status.tsx
autonomous: false
requirements: [PIPE-04, PIPE-06]

must_haves:
  truths:
    - "Each feed card displays a content type badge when contentType is set (news, fan art, meme, video, discussion, translation, official)"
    - "Users can filter the feed by content type via a filter control"
    - "Status page shows pipeline section with stats: items processed, approved, rejected, pending"
    - "Status page shows LLM cost tracking (tokens used, estimated spend)"
    - "Status page shows fallback mode indicator when pipeline is in fallback"
  artifacts:
    - path: "packages/frontend/src/components/FeedCard.tsx"
      provides: "Content type badge on feed cards"
      contains: "contentType"
    - path: "packages/frontend/src/pages/News.tsx"
      provides: "Content type filter for feed"
      contains: "contentType"
    - path: "packages/frontend/src/pages/Status.tsx"
      provides: "Pipeline stats section on status page"
      contains: "pipeline"
  key_links:
    - from: "packages/frontend/src/pages/Status.tsx"
      to: "/api/pipeline/stats"
      via: "fetch in useEffect"
      pattern: "pipeline/stats"
    - from: "packages/frontend/src/pages/News.tsx"
      to: "/api/feed?contentType="
      via: "fetch with query parameter"
      pattern: "contentType"
    - from: "packages/frontend/src/components/FeedCard.tsx"
      to: "FeedItem.contentType"
      via: "badge render"
      pattern: "contentType"
---

<objective>
Add content type badges to feed cards, content type filtering to the feed, and pipeline monitoring to the status page.

Purpose: Users need to see what type each piece of content is (news, fan art, meme, etc.) and filter by type. The status page is the primary monitoring surface for pipeline health, cost tracking, and fallback warnings — essential for operational visibility.

Output: Feed cards with content type badges, content type filter on the feed page, and pipeline stats section on the status page.
</objective>

<execution_context>
@/home/kylesulit/.claude/get-shit-done/workflows/execute-plan.md
@/home/kylesulit/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-llm-moderation-pipeline/07-CONTEXT.md
@.planning/phases/07-llm-moderation-pipeline/07-02-SUMMARY.md

Key existing files to read before modifying:
@packages/frontend/src/components/FeedCard.tsx
@packages/frontend/src/pages/News.tsx
@packages/frontend/src/pages/Status.tsx
@packages/frontend/src/App.tsx
@packages/shared/src/types/feed.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add content type badges to feed cards and content type filter to feed</name>
  <files>
    packages/frontend/src/components/FeedCard.tsx
    packages/frontend/src/pages/News.tsx
  </files>
  <action>
1. In `packages/frontend/src/components/FeedCard.tsx`:
   - Add a content type badge that displays when `item.contentType` is not null
   - Badge should appear near the source info (after the source detail text or in the card header area)
   - Define a `contentTypeBadgeColors` map for visual distinction:
     - news: blue (#3b82f6)
     - fan_art: pink (#ec4899)
     - meme: yellow (#eab308)
     - video: red (#ef4444)
     - discussion: green (#22c55e)
     - translation: purple (#a855f7)
     - official: gold (#f59e0b)
   - Define a `contentTypeLabels` map for display names:
     - news: "News", fan_art: "Fan Art", meme: "Meme", video: "Video", discussion: "Discussion", translation: "Translation", official: "Official"
   - Badge styling: small pill-shaped badge (fontSize 11, padding 2px 8px, borderRadius 10, background color with 20% opacity, text color matching the badge color, fontWeight 500)
   - Only render the badge if contentType is truthy (it will be null for items that haven't been classified yet, including existing items and fallback-approved items)

2. In `packages/frontend/src/pages/News.tsx` (or wherever the feed filter/source tabs live):
   - Add a content type filter row below the existing source filter tabs
   - Content type options: All, News, Fan Art, Meme, Video, Discussion, Translation, Official
   - Styled as small pill buttons similar to the existing source tabs but visually distinct (smaller, secondary appearance)
   - When a content type is selected:
     - If using the v2 API (VITE_API_URL is set): append `?contentType={type}` to the feed API call (the server now supports this filter per Plan 02)
     - If using v1 client-side mode: filter items client-side by `item.contentType === selectedType`
   - Default to "All" (no content type filter)
   - Match the existing filter interaction pattern in the codebase (likely useState for selectedContentType)
   - Use the same `contentTypeLabels` and `contentTypeBadgeColors` maps from FeedCard (extract to a shared constants object imported by both components, or define in a small utility file if needed)
  </action>
  <verify>
Run `npx tsc --noEmit` in packages/frontend. Visually inspect that FeedCard renders a content type badge when contentType is set. Verify content type filter pills render on the feed page.
  </verify>
  <done>
Feed cards show content type badges (news=blue, fan_art=pink, etc.) when classified. Feed page has content type filter pills that filter the feed by type. Unclassified items show no badge. Filter defaults to "All" showing everything.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add pipeline stats section to status page</name>
  <files>
    packages/frontend/src/pages/Status.tsx
  </files>
  <action>
1. In `packages/frontend/src/pages/Status.tsx`:
   - Add a new "Pipeline" section ABOVE the existing "Source Status" section
   - Fetch from `${apiUrl}/api/pipeline/stats` alongside the existing health fetch (can be a separate fetch in the same useEffect, or a parallel Promise.all)
   - Define a `PipelineStats` interface matching the API response:
     ```
     { totalRuns, todayRuns, itemsProcessed, itemsApproved, itemsRejected,
       totalInputTokens, totalOutputTokens, estimatedCostUsd,
       inFallbackMode, contentCounts: { raw, pending, approved, rejected },
       lastRunAt, provider }
     ```
   - Pipeline section card layout:
     a. **Header row:** "LLM Pipeline" title with a status indicator:
        - Green dot + "Active" if lastRunAt exists and not in fallback
        - Yellow dot + "Fallback Mode" if inFallbackMode is true (per user decision — visible indicator when pipeline is degraded)
        - Gray dot + "No Runs" if no runs yet
     b. **Stats row:** 4 stat boxes in a 2x2 grid:
        - Processed: itemsProcessed (total)
        - Approved: itemsApproved with green accent
        - Rejected: itemsRejected with red accent
        - Pending: contentCounts.pending with yellow accent
     c. **Cost row:** Single row showing:
        - Provider name (e.g., "openai/gpt-4.1-nano")
        - Tokens: `${totalInputTokens} in / ${totalOutputTokens} out`
        - Cost: `$${estimatedCostUsd}` (format to 4 decimal places)
        - Today's runs: todayRuns
     d. **Fallback warning banner** (only shown when inFallbackMode === true):
        - Yellow/amber background banner: "Pipeline in fallback mode — LLM budget exceeded or provider unavailable. BTS-focused sources auto-approved, broad sources queued."
   - Style consistently with the existing source status cards (same card background #1f2937, same border radius, same color scheme)
   - Handle loading state with a skeleton placeholder matching the pipeline card shape
   - Handle fetch error gracefully (show "Pipeline stats unavailable" message, don't break the source status section)
   - Auto-refresh on the same 60-second interval as the health data
  </action>
  <verify>
Run `npx tsc --noEmit` in packages/frontend. Verify Status.tsx compiles and the pipeline section renders with the expected layout. Verify fallback mode banner only shows when inFallbackMode is true.
  </verify>
  <done>
Status page shows pipeline section with processed/approved/rejected/pending counts, provider info, token usage, cost tracking, and fallback mode warning. Auto-refreshes every 60s. Gracefully handles missing pipeline data.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify LLM moderation pipeline end-to-end</name>
  <files>none</files>
  <action>
Human verification of the complete LLM moderation pipeline: schema with moderation_status, provider factory, batched classification, budget tracking, fallback mode, admin API, feed filtering to approved-only, content type badges on feed cards, content type filter, and pipeline stats on status page.

Steps to verify:
1. Start the server: `cd packages/server && npx tsx src/index.ts`
   - Verify it starts without errors
   - Verify initial scrape triggers and pipeline runs after scraping (check logs for "[pipeline]" messages)

2. Check the feed: `curl http://localhost:3001/api/feed?limit=5`
   - Verify items have `contentType` populated (non-null for LLM-classified items)
   - Verify all returned items would have moderation_status = 'approved' (the feed should NOT return raw/pending/rejected items)

3. Check pipeline stats: `curl http://localhost:3001/api/pipeline/stats`
   - Verify response includes itemsProcessed, itemsApproved, itemsRejected, token counts, cost estimate

4. Check rejected items: `curl http://localhost:3001/api/pipeline/rejected`
   - Verify response is an array (may be empty if all items were approved)

5. Test content type filter: `curl http://localhost:3001/api/feed?contentType=news`
   - Verify only news-type items are returned

6. Open the frontend (with VITE_API_URL set):
   - Verify feed cards show content type badges (colored pills)
   - Verify content type filter pills are visible and functional
   - Visit /status page — verify pipeline section shows stats, cost, provider info
   - If in fallback mode, verify the yellow warning banner appears

7. Test fallback mode by setting `LLM_DAILY_BUDGET=0` and triggering a scrape:
   - Verify BTS-focused source items are auto-approved
   - Verify broad-source items are queued as pending
   - Verify status page shows "Fallback Mode" indicator

Resume: Type "approved" or describe issues to fix.
  </action>
  <verify>Human visual and functional verification of end-to-end pipeline.</verify>
  <done>Pipeline processes content through raw -> approved/rejected, feed shows only approved items with content type badges, status page shows pipeline stats and fallback indicator, content type filter works.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes in packages/frontend
2. FeedCard renders content type badge when contentType is set
3. News page has content type filter pills
4. Status page shows pipeline section with stats, cost, fallback indicator
5. Feed API filters to approved-only items
6. Content type filter works via ?contentType= query parameter
7. End-to-end pipeline processes new items after scraping
</verification>

<success_criteria>
- Content type badges visible on feed cards (PIPE-04 display)
- Users can filter feed by content type (PIPE-04 user interaction)
- Status page shows pipeline stats, cost tracking, and fallback indicator (PIPE-06 visibility)
- Complete end-to-end verification: scrape -> pipeline -> approved items in feed with badges
</success_criteria>

<output>
After completion, create `.planning/phases/07-llm-moderation-pipeline/07-03-SUMMARY.md`
</output>
