---
phase: 04-config-driven-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/config/types.ts
  - src/config/groups/bts/index.ts
  - src/config/groups/bts/labels.ts
  - src/config/groups/bts/events.ts
  - src/config/groups/bts/news.ts
  - src/types/feed.ts
  - src/data/events.ts
  - src/data/news.ts
  - src/data/members.ts
autonomous: true
requirements: [CONFIG-03, CONFIG-04]

must_haves:
  truths:
    - "GroupConfig has a required `labels` field with all user-facing strings"
    - "GroupConfig has required `events` and `news` fields for group-specific data"
    - "FeedSource and BiasId are string types, not hardcoded unions"
    - "BTS config compiles with `satisfies GroupConfig` including all new required fields"
    - "No import of src/data/events.ts, src/data/news.ts, or src/data/members.ts exists outside config"
  artifacts:
    - path: "src/config/types.ts"
      provides: "GroupLabels interface, updated GroupConfig with labels/events/news fields"
      contains: "GroupLabels"
    - path: "src/config/groups/bts/labels.ts"
      provides: "BTS-specific labels (appName, sourceLabels, memberFilterLabel, homeQuote, tourTitle, etc.)"
      contains: "sourceLabels"
    - path: "src/config/groups/bts/events.ts"
      provides: "BTS tour events data (moved from src/data/events.ts)"
      contains: "events"
    - path: "src/config/groups/bts/news.ts"
      provides: "BTS fallback news data (moved from src/data/news.ts)"
      contains: "news"
    - path: "src/types/feed.ts"
      provides: "FeedSource as string, BiasId as string"
      contains: "export type FeedSource = string"
  key_links:
    - from: "src/config/groups/bts/index.ts"
      to: "src/config/types.ts"
      via: "satisfies GroupConfig"
      pattern: "satisfies GroupConfig"
    - from: "src/config/groups/bts/index.ts"
      to: "src/config/groups/bts/labels.ts"
      via: "import labels"
      pattern: "import.*labels"
---

<objective>
Extend GroupConfig type system with labels, events, and news fields, then populate BTS config with all group-specific data and loosen FeedSource/BiasId to string types.

Purpose: Create the type foundation that makes every UI element and build artifact derivable from config, enabling the clone-and-swap architecture.
Output: Updated GroupConfig with labels/events/news, BTS labels data, migrated events/news data, string-typed FeedSource/BiasId.
</objective>

<execution_context>
@/home/kylesulit/.claude/get-shit-done/workflows/execute-plan.md
@/home/kylesulit/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-config-driven-ui/04-RESEARCH.md
@src/config/types.ts
@src/config/groups/bts/index.ts
@src/types/feed.ts
@src/data/events.ts
@src/data/news.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend GroupConfig types and create BTS labels</name>
  <files>
    src/config/types.ts
    src/config/groups/bts/labels.ts
    src/config/groups/bts/events.ts
    src/config/groups/bts/news.ts
    src/config/groups/bts/index.ts
  </files>
  <action>
1. In `src/config/types.ts`:
   - Add `GroupLabels` interface with ALL required string fields (no optionals):
     - `appName: string` ("BTS ARMY App")
     - `appTitle: string` ("BTS - ARMY App" for browser tab)
     - `appDescription: string` ("BTS Fan App for ARMY")
     - `sourceLabels: Record<string, string>` (maps source type to display label, e.g., `{ reddit: "Reddit", youtube: "YouTube", rss: "News", twitter: "Twitter", tumblr: "Tumblr" }`)
     - `memberFilterLabel: string` ("Select Your Bias")
     - `homeQuote: string` ("Love yourself, speak yourself")
     - `tourTitle: string` ("BTS WORLD TOUR 'ARIRANG'")
     - `tourSubtitle: string` ("82+ shows across 34 cities in 23 countries")
   - Add `Event` interface (move from `src/data/events.ts` -- same shape: id, title, date, venue, city, region, description, status, ticketUrl?)
   - Add `NewsItem` interface (move from `src/data/news.ts` -- same shape: id, headline, date, summary, source, sourceUrl)
   - Add `labels: GroupLabels`, `events: Event[]`, and `news: NewsItem[]` as required fields on `GroupConfig`

2. Create `src/config/groups/bts/labels.ts`:
   - Export `labels` object with all BTS-specific label values as listed above
   - Use `satisfies GroupLabels` for compile-time validation

3. Create `src/config/groups/bts/events.ts`:
   - Move the entire `events` array from `src/data/events.ts` into this file
   - Import `Event` type from `../../types.ts`
   - Export `events` typed as `Event[]`

4. Create `src/config/groups/bts/news.ts`:
   - Move the entire `news` array from `src/data/news.ts` into this file
   - Import `NewsItem` type from `../../types.ts`
   - Export `news` typed as `NewsItem[]`

5. Update `src/config/groups/bts/index.ts`:
   - Import `labels` from `./labels.ts`
   - Import `events` from `./events.ts`
   - Import `news` from `./news.ts`
   - Add `labels`, `events`, `news` to the `btsConfig` object (alongside existing members, sources, theme, keywords)
   - Verify `satisfies GroupConfig` still compiles

NOTE: Do NOT delete `src/data/events.ts`, `src/data/news.ts`, or `src/data/members.ts` yet -- they are still imported by pages. Plan 02 will update the imports and delete the old files.
  </action>
  <verify>Run `npx tsc --noEmit` to confirm type system compiles without errors. Verify `btsConfig` satisfies the expanded `GroupConfig` type.</verify>
  <done>GroupConfig has required labels/events/news fields. BTS config provides all label values, events data, and news data. TypeScript compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: Loosen FeedSource and BiasId to string types</name>
  <files>
    src/types/feed.ts
  </files>
  <action>
1. In `src/types/feed.ts`:
   - Change `FeedSource` from `"reddit" | "youtube" | "news" | "rss" | "twitter" | "tumblr"` to `string`
   - Change `BiasId` from `"rm" | "jin" | "suga" | "jhope" | "jimin" | "v" | "jungkook"` to `string`
   - Remove the `// TODO: Move BiasId to config-derived type in Plan 04` comment (we are fulfilling it now)
   - Keep `VideoType`, `FeedStats`, and `FeedItem` interfaces unchanged

2. Verify no type errors are introduced. The code already handles unknown source types gracefully:
   - `sourceBadgeColors` and `sourceEmojis` in FeedCard/SwipeFeed are plain objects that return `undefined` for unknown keys
   - `getEngagementValue` in feeds.ts has a `default: return 0` case
   - BiasFilter casts `m.id as BiasId` which now just becomes `m.id as string` (no-op)

3. Run `npx tsc --noEmit` to confirm full project compiles.
  </action>
  <verify>Run `npx tsc --noEmit` -- zero type errors. Run `npm run build` -- successful production build.</verify>
  <done>FeedSource and BiasId are string types. All existing code compiles without errors. Build succeeds.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with zero errors
2. `npm run build` produces a successful production build
3. `src/config/types.ts` contains GroupLabels interface with all required fields
4. `src/config/groups/bts/index.ts` includes labels, events, news in btsConfig
5. `src/types/feed.ts` has `FeedSource = string` and `BiasId = string`
</verification>

<success_criteria>
- GroupConfig type requires labels, events, and news fields -- TypeScript errors if any group config omits them
- BTS config satisfies expanded GroupConfig with all labels populated
- Events and news data exist in group config directory alongside members/sources/theme
- FeedSource and BiasId are string types, enabling config-driven filter tabs and member chips
- Full project builds successfully
</success_criteria>

<output>
After completion, create `.planning/phases/04-config-driven-ui/04-01-SUMMARY.md`
</output>
