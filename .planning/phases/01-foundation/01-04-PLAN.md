---
phase: 01-foundation
plan: 04
type: execute
wave: 3
depends_on: ["01-02", "01-03"]
files_modified:
  - src/hooks/useFeed.ts
  - src/hooks/useBias.ts
  - src/components/BiasFilter.tsx
  - src/components/FeedCard.tsx
  - src/components/SwipeFeed.tsx
  - src/pages/Home.tsx
  - src/pages/News.tsx
  - src/pages/Members.tsx
  - src/pages/MemberDetail.tsx
  - src/App.css
  - src/config/applyTheme.ts
autonomous: true
requirements:
  - CONFIG-02

must_haves:
  truths:
    - "Grep for hardcoded BTS references in service/component/hook code returns zero matches"
    - "Every BTS-specific keyword, subreddit name, channel ID, and member data value comes from the GroupConfig object"
    - "CSS custom properties use generic --theme-* names applied from config at runtime"
    - "The app produces identical visual results as before the refactor"
  artifacts:
    - path: "src/config/applyTheme.ts"
      provides: "Runtime CSS custom property application from ThemeConfig"
      exports: ["applyTheme"]
    - path: "src/hooks/useFeed.ts"
      provides: "Feed hook reading member keywords from config"
      contains: "getConfig"
    - path: "src/hooks/useBias.ts"
      provides: "Bias hook with config-derived storage key"
      contains: "getConfig"
  key_links:
    - from: "src/hooks/useFeed.ts"
      to: "src/config"
      via: "reads member keywords from config for bias filtering"
      pattern: "getConfig|config\\.members"
    - from: "src/components/BiasFilter.tsx"
      to: "src/config"
      via: "reads member data for chip rendering"
      pattern: "getConfig|config\\.members"
    - from: "src/config/applyTheme.ts"
      to: "document.documentElement"
      via: "setProperty for CSS custom properties"
      pattern: "setProperty.*--theme"
    - from: "src/pages/Home.tsx"
      to: "src/config"
      via: "reads group name, tagline, logo from config"
      pattern: "config\\.theme"
---

<objective>
Wire the GroupConfig throughout the entire app, eliminating all hardcoded BTS references in hooks, components, and pages. Apply theme colors from config at runtime via CSS custom properties.

Purpose: Complete the config-driven architecture so that zero hardcoded BTS references remain in service/component/hook code, making the app fully clone-and-swappable.
Output: All hooks, components, and pages read group-specific data from config instead of hardcoded values.
</objective>

<execution_context>
@/home/kylesulit/.claude/get-shit-done/workflows/execute-plan.md
@/home/kylesulit/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
@.planning/phases/01-foundation/01-03-SUMMARY.md
@src/config/index.ts
@src/config/types.ts
@src/hooks/useFeed.ts
@src/hooks/useBias.ts
@src/components/BiasFilter.tsx
@src/components/FeedCard.tsx
@src/components/SwipeFeed.tsx
@src/pages/Home.tsx
@src/pages/News.tsx
@src/pages/Members.tsx
@src/pages/MemberDetail.tsx
@src/App.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire config into hooks and create theme application</name>
  <files>
    src/hooks/useFeed.ts
    src/hooks/useBias.ts
    src/config/applyTheme.ts
    src/App.css
    src/main.tsx
  </files>
  <action>
1. **`src/hooks/useFeed.ts`**:
   - Import `getConfig` from `../config`.
   - Replace the hardcoded `CACHE_KEY = "bts-feed-cache"` with a config-derived key: use `config.theme.groupName.toLowerCase()` to build the key, e.g., `${config.theme.groupName.toLowerCase()}-feed-cache`.
   - Replace the `matchesBias` function: instead of importing `MEMBER_KEYWORDS` from `../types/feed`, read member aliases from `config.members`. Build the keyword lookup from `config.members.find(m => m.id === biasId)?.aliases`.
   - Remove the import of `MEMBER_KEYWORDS` from `../types/feed`.
   - Keep all other behavior identical (cache TTL, incremental loading, filter logic).

2. **`src/hooks/useBias.ts`**:
   - Import `getConfig` from `../config`.
   - Replace the hardcoded `STORAGE_KEY = "bts-bias-selection"` with a config-derived key: `${config.theme.groupName.toLowerCase()}-bias-selection`.
   - Keep all other behavior identical.

3. **`src/config/applyTheme.ts`** -- Create new file:
   - Import `ThemeConfig` from `./types`.
   - Export `applyTheme(theme: ThemeConfig): void` function.
   - Set CSS custom properties on `document.documentElement.style`:
     - `--theme-primary` = theme.primaryColor
     - `--theme-accent` = theme.accentColor
     - `--theme-dark` = theme.darkColor
   - This replaces the hardcoded `--bts-purple-*` variables.

4. **`src/App.css`**:
   - Rename ALL `--bts-purple` variables to generic `--theme-*` names:
     - `--bts-purple` -> `--theme-primary`
     - `--bts-purple-light` -> `--theme-accent`
     - `--bts-purple-dark` -> `--theme-dark`
   - Update ALL references to these variables throughout App.css.
   - Keep the variable declarations in `:root` as fallback defaults (the runtime applyTheme will override them).
   - Search the entire `src/` directory for any other CSS files or inline styles referencing `--bts-purple` and update those too.

5. **`src/main.tsx`**:
   - Import `applyTheme` from `./config/applyTheme` and `getConfig` from `./config`.
   - Call `applyTheme(getConfig().theme)` before React renders (at module scope or in the entry point before `createRoot`).
  </action>
  <verify>
    - `npx tsc --noEmit` compiles without errors
    - `grep -r "bts-feed-cache\|bts-bias-selection\|MEMBER_KEYWORDS" src/hooks/` returns zero matches
    - `grep -r "bts-purple" src/` returns zero matches (all renamed to --theme-*)
    - `src/config/applyTheme.ts` exists and exports applyTheme
    - `src/main.tsx` calls applyTheme
  </verify>
  <done>
    Hooks read member keywords and storage keys from config. CSS uses generic --theme-* variables applied at runtime from config. No "bts-" prefixed localStorage keys or CSS variables remain.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire config into components and pages</name>
  <files>
    src/components/BiasFilter.tsx
    src/components/FeedCard.tsx
    src/components/SwipeFeed.tsx
    src/pages/Home.tsx
    src/pages/News.tsx
    src/pages/Members.tsx
    src/pages/MemberDetail.tsx
  </files>
  <action>
Read each file listed below, then make the specified changes. For each file, import `getConfig` from the appropriate relative path to `../config` or `../../config`.

1. **`src/components/BiasFilter.tsx`**:
   - Remove the hardcoded `memberChips` array (member IDs, labels, emojis, colors).
   - Instead, derive chips from `config.members`: map each MemberConfig to a chip object using `m.id`, `m.stageName`, `m.emoji`, `m.color`.
   - Keep the component's existing rendering logic and props interface.

2. **`src/components/FeedCard.tsx`**:
   - Remove the hardcoded `sourceBadgeColors` object.
   - Either derive badge colors from a config mapping, or make them generic based on source type. Since source badge colors are UI presentation (not group-specific data), these can stay as a local constant BUT should use generic names, not BTS-specific ones. Read the current file to see if the colors are BTS-themed or source-themed. If they're source-themed (e.g., "reddit" -> orange, "youtube" -> red), they can stay as-is since they're not BTS-specific.

3. **`src/components/SwipeFeed.tsx`**:
   - Same treatment as FeedCard.tsx for `sourceBadgeColors`.
   - Remove the hardcoded `sourceEmojis` object and derive from config or make generic.

4. **`src/pages/Home.tsx`**:
   - Replace hardcoded "BTS", "방탄소년단", "Beyond The Scene · ARMY Forever", and group photo path with reads from `config.theme.groupName`, `config.theme.groupNameNative`, `config.theme.tagline`, `config.theme.logoUrl`.
   - Replace "BTS content from the web" with `${config.theme.groupName} content from the web`.

5. **`src/pages/News.tsx`**:
   - Replace "BTS content from across the web" with `${config.theme.groupName} content from across the web`.
   - Replace the hardcoded `@BTS_twt` social link with `config.theme.socialLinks`.

6. **`src/pages/Members.tsx`**:
   - Replace "The 7 members of BTS" with a config-driven string: `The ${config.members.length} members of ${config.theme.groupName}`.
   - Import member data from config instead of `../data/members` if members page uses that.

7. **`src/pages/MemberDetail.tsx`**:
   - Remove the hardcoded `memberEmojis` mapping (member ID to emoji).
   - Instead, look up the member's emoji from `config.members.find(m => m.id === memberId)?.emoji`.
   - Import member data from config instead of `../data/members` if this page uses that.

NOTE on FeedFilter.tsx: The filter tabs array (source type labels) is Phase 4 scope (CONFIG-03). Do NOT modify FeedFilter.tsx in this plan. The source data it needs is available in config, but the UI generation is deferred.

NOTE on src/data/members.ts: Keep this file for now -- it may still be imported by pages. But pages should be updated to import from config instead. The old data file can be deleted in a later cleanup or kept as a reference.

After all changes, do a final audit:
- `grep -rn "BTS\|bangtan\|방탄\|bts-\|ARMY" src/components/ src/hooks/ src/pages/ src/services/` -- check every match.
- Matches in config files (src/config/) are OK (that's where BTS data lives).
- Matches in code files should be zero, OR should be reading from config (e.g., `config.theme.groupName` which evaluates to "BTS" at runtime is fine).
- Matches in static data files (src/data/) can remain for now (they'll be deprecated as config takes over).
  </action>
  <verify>
    - `npx tsc --noEmit` compiles without errors
    - `npm run build` succeeds
    - `grep -rn "BTS\|방탄소년단\|@BTS_twt\|bts-purple\|memberEmojis\|memberChips\|sourceBadgeColors\|sourceEmojis" src/components/ src/hooks/ src/pages/ src/services/` returns zero matches for hardcoded BTS references (config reads are OK)
    - BiasFilter renders member chips from config data
    - Home page renders group name, tagline, logo from config
    - News page renders social links from config
  </verify>
  <done>
    Zero hardcoded BTS references remain in components, hooks, pages, or services. All group-specific data is read from the GroupConfig object. The app renders identically but all data flows through config. Running the app with a different config would swap the entire branding.
  </done>
</task>

</tasks>

<verification>
- `npm run build` succeeds with zero errors
- `npx tsc --noEmit` passes type checking
- Comprehensive audit: `grep -rn` for BTS-specific strings in src/components/, src/hooks/, src/pages/, src/services/ returns only config reads, not hardcoded values
- CSS uses --theme-* variables (no --bts-* variables anywhere)
- App.css has generic variable names
- applyTheme is called at app startup
- Members pages import data from config, not from src/data/members.ts
</verification>

<success_criteria>
- Every BTS-specific keyword, name, color, emoji, URL, and data value in service/component/hook code comes from the GroupConfig object
- CSS custom properties use generic --theme-* names applied from config at runtime
- Grep for hardcoded BTS references in non-config code returns zero matches
- The app produces identical visual results as before the refactor (same colors, same text, same data)
- The project builds and type-checks cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-04-SUMMARY.md`
</output>
